## Verify the SSL cert when connecting to models (default: True)
verify-ssl: true

# Cache settings:
## Enable caching of prompts (default: False)
cache-prompts: true

## Specify the chat input history file (default: .aider.input.history)
input-history-file: .aider.input.history

## Specify the chat history file (default: .aider.chat.history.md)
chat-history-file: .aider.chat.history.md

## Use colors suitable for a dark terminal background (default: False)
dark-mode: true

## Use colors suitable for a light terminal background (default: False)
#light-mode: false

## Enable/disable pretty, colorized output (default: True)
pretty: true

## Enable/disable streaming responses (default: True)
stream: true

# timeout: 60

user-input-color: "#A6E3A1" # Catppuccin green (Foreground)
tool-output-color: "#89B4FA" # Catppuccin blue (Accent)
tool-error-color: "#F38BA8" # Catppuccin rosewater (Muted Text)
tool-warning-color: "#FAB387" # Catppuccin peach (Highlight)
assistant-output-color: "#89DCEB" # Catppuccin lavender (Accent)
completion-menu-color: "#CBA6F7" # Catppuccin teal (Foreground)
completion-menu-bg-color: "#1E1E2E" # Catppuccin background (Background)
completion-menu-current-color: "#F5E0DC" # Catppuccin rosewater (Muted Text)
completion-menu-current-bg-color: "#302D41" # Catppuccin surface1 (Selection)

## Enable/disable looking for a git repo (default: True)
git: true

## Enable/disable adding .aider* to .gitignore (default: True)
gitignore: true

## Specify the aider ignore file (default: .aiderignore in git root)
aiderignore: .aiderignore

## Only consider files in the current subtree of the git repository
subtree-only: false

## Enable/disable auto commit of LLM changes (default: True)
auto-commits: false

## Enable/disable commits when repo is found dirty (default: True)
#dirty-commits: true

## Attribute aider code changes in the git author name (default: True)
attribute-author: true

## Attribute aider commits in the git committer name (default: True)
attribute-committer: true

## Prefix commit messages with 'aider: ' if aider authored the changes (default: False)
attribute-commit-message-author: false

## Prefix all commit messages with 'aider: ' (default: False)
attribute-commit-message-committer: false

## Commit all pending changes with a suitable commit message, then exit
#commit: false

## Specify a custom prompt for generating commit messages
#commit-prompt: xxx

## Perform a dry run without modifying files (default: False)
#dry-run: false

## Skip the sanity check for the git repository (default: False)
#skip-sanity-check-repo: false

## Enable/disable watching files for ai coding comments (default: False)
watch-files: true

## Enable/disable automatic linting after changes (default: True)
auto-lint: false

## Check for new aider versions on launch
check-update: true

## Audio format for voice recording (default: wav). webm and mp3 require ffmpeg
voice-format: wav

## Specify the language for voice using ISO 639-1 code (default: auto)
voice-language: en

## Use VI editing mode in the terminal (default: False)
vim: true

## Always say yes to every confirmation
yes-always: false

## Enable verbose output
verbose: false

## Enable/disable suggesting shell commands (default: True)
suggest-shell-commands: true

## Enable/disable fancy input with history and completion (default: True)
fancy-input: true

## Enable/disable multi-line input mode with Meta-Enter to submit (default: False)
multiline: false

## Specify which editor to use for the /editor command
editor: nvim
